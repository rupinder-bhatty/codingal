"""
1) Add the project title and topics.
   a) Label the program as "Array Challenge Lab".
   b) Mention topics like two-pointer swap, reverse in groups, left rotation, and leaders in an array.

2) Reverse an array using two pointers.
   a) Create a list of scores.
   b) Set one pointer at the start and one at the end.
   c) Swap values while the start pointer is less than the end pointer.
   d) Move the start pointer forward and the end pointer backward.
   e) Print the reversed list.

3) Reverse the array in groups.
   a) Create a new list of scores.
   b) Set the group size as 3.
   c) Loop through the list group by group.
   d) Reverse each group using two pointers.
   e) Print the updated list.

4) Rotate the array left by N positions.
   a) Create a list of scores.
   b) Repeat the left rotation 2 times.
   c) Store the first element in a temporary variable.
   d) Shift all remaining elements one position to the left.
   e) Place the first element at the end.
   f) Print the rotated list.

5) Find leaders in an array.
   a) Create a list of scores.
   b) Start checking from the right side of the list.
   c) Keep track of the maximum value seen so far.
   d) Add a score to leaders if it is greater than all scores to its right.
   e) Reverse the leaders list to show them in original order.
   f) Print the scores and leaders.
"""