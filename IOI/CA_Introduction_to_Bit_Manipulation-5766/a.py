"""
1) Add the project title and topics.
   a) Label the program as "Bit Explorer".
   b) Mention topics like bits, binary, AND, OR, NOT, XOR, shifts, odd/even, and counting bits.

2) Create two numbers for bit operations.
   a) Store `10` in variable `a`.
   b) Store `6` in variable `b`.
   c) Note their binary forms.

3) Create the `bits()` helper function.
   a) Take a number and width as input.
   b) Convert the number into binary format.
   c) Return the binary value with fixed width.

4) Display binary values.
   a) Print the project heading.
   b) Print `a` and `b` with their binary forms.

5) Demonstrate AND and OR.
   a) Use `a & b` to show bitwise AND.
   b) Use `a | b` to show bitwise OR.
   c) Print both decimal and binary results.

6) Demonstrate NOT and XOR.
   a) Use `~a` to flip bits.
   b) Use `a ^ b` to compare different bits.
   c) Print both decimal and binary results.

7) Demonstrate shift operators.
   a) Use `a << 1` to shift left and double the value.
   b) Use `a >> 1` to shift right and halve the value.

8) Check odd or even using XOR.
   a) Loop through a list of numbers.
   b) Use XOR logic to identify whether each number is odd or even.
   c) Print the result.

9) Create the `count_bits()` function.
   a) Start the count from 0.
   b) Use a `while` loop until the number becomes 0.
   c) Increase the count for each shift.
   d) Shift the number right by 1 each time.

10) Display bit counts.
   a) Loop through selected numbers.
   b) Print how many bits each number needs.
   c) Show the binary form of each number.
"""