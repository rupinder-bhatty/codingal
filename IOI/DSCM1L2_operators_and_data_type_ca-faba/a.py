"""
1) Add the project title and file name.
   a) Use comments to label the program as "My Snack Shop".
   b) Mention the file name as `my-snack-shop.py`.

2) Create variables with different data types.
   a) Store the snack name as a string.
   b) Store the price as a float.
   c) Store the quantity as an integer.
   d) Store availability as a Boolean value.

3) Print the snack details.
   a) Display the snack name, price, stock quantity, and availability.
   b) Use `type()` to show the data type of each variable.

4) Perform arithmetic operations.
   a) Multiply price and quantity to find the total value.
   b) Subtract from the price to show a sale price.
   c) Multiply quantity to show double stock.

5) Use comparison operators.
   a) Check if the price is under 2 dollars.
   b) Check if more than 5 snacks are in stock.
   c) Check if the price is exactly 1.50.

6) Use string operations.
   a) Join words to create the shop name.
   b) Use `len()` to count letters in the snack name.
   c) Use indexing to print the first letter.

7) Swap two values.
   a) Create two price variables.
   b) Print the values before swapping.
   c) Use a temporary variable to swap them.
   d) Print the values after swapping.
"""