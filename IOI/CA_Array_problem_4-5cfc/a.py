"""
1) Add the file name.
   a) Use a comment to mention the activity file name.

2) Work with slices and sums.
   a) Create an array with numbers.
   b) Print the full array.
   c) Print the elements left of a chosen index.
   d) Print the elements right of the chosen index.
   e) Use `sum()` to calculate left and right sums.

3) Check balance at every index.
   a) Loop through every index in the array.
   b) Calculate the sum of elements on the left.
   c) Calculate the sum of elements on the right.
   d) Print both sums for each index.

4) Find the equilibrium point.
   a) Loop through every index again.
   b) Compare the left sum and right sum.
   c) Print the index and element when both sums are equal.

5) Create a growing subarray window.
   a) Create a new list of numbers.
   b) Set a target sum.
   c) Start the current sum from 0.
   d) Keep adding elements from a fixed start index.
   e) Stop when the current sum reaches or crosses the target.

6) Search for a subarray with target sum.
   a) Use nested loops to try different start and end positions.
   b) Add numbers to the current window sum.
   c) Print the subarray when the sum equals the target.
   d) Stop early if the sum becomes greater than the target.
   e) Print a message if no subarray is found.
"""